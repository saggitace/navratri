{
  "crons": [
    {
      "path": "/api/cleanup-expired-bookings",
      "schedule": "*/10 * * * *",
      "headers": {
        "Authorization": "Bearer ${process.env.CRON_SECRET}"
      }
    }
  ]
}